# secrets — never commit
.env
*.pem

# python
__pycache__/
*.py[cod]
.venv/
venv/

# model caches downloaded by livekit plugins
.cache/

# intermediate audio (only final .ogg/.mp3 are deliverables)
*.wav
*.raw
scratch/

# analyzer scratch backup, not a deliverable
BUG_REPORT.prev.md
